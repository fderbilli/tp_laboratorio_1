/**
*\brief Realiza la suma de dos operandos que el usuario introdujo.
*\param Operando 1. Primer operando/numero que posteriormente va a ser sumado.
*\param Operando 2. Segundo operando/numero que posteriormente va a ser sumado.
*\return Resultado de la suma del operando 1 y el operando 2.
*
*/
float suma(float operando1, float operando2);
/**
*\brief Realiza la resta de dos operandos que el usuario introdujo.
*\param Operando 1. Primer operando/numero que posteriormente va a ser restado.
*\param Operando 2. Segundo operando/numero que posteriormente va a ser restado.
*\return Resultado de la resta del operando 1 y el operando 2.
*
*/
float resta (float operando1, float operando2);
/**
*\brief Realiza la multiplicación de dos operandos que el usuario introdujo.
*\param Operando 1. Primer operando/numero que posteriormente va a ser sumado.
*\param Operando 2. Segundo operando/numero que posteriormente va a ser sumado.
*\return Resultado de la multiplicación del operando 1 y el operando 2.
*
*/
float multiplicar(float operando1, float operando2);
/**
*\brief Realiza la division de dos operandos que el usuario introdujo.
*\param Operando 1. Primer operando/numero que posteriormente va a ser dividido.
*\param Operando 2. Segundo operando/numero que posteriormente va a ser dividido.
*\return Resultado de la division del operando 1 y el operando 2.
*
*/
float dividir(float operando1, float operando2);
/**
*\brief Realiza el factorial de un operando que el usuario introdujo.
*\param Operando 1.  Operando/numero que posteriormente va a dar como resultado el factorial.
*\return Resultado del factorial del operando que se introdujo..
*
*/
float factorial(float operando1);
